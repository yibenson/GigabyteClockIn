<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ClockIn" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main</item>
        <item name="colorPrimaryVariant">@color/main_light</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/main_deep</item>
        <item name="android:colorBackground">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/main</item>
        <item name="colorSecondaryVariant">@color/main_deep</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/main_deep</item>
        <!-- Customize your theme here. -->
        <item name="android:datePickerStyle">@style/MyDatePicker</item>
        <item name="buttonStyle">@style/Button</item>
        <item name="editTextStyle">@style/MyEditText</item>
        <item name="android:textViewStyle">@style/MyTextView</item>
        <item name="actionBarStyle">@style/MyActionBarStyle</item>
"
    </style>

    <!--to toggle the open close inactive_button.png
        of the navigation drawer-->
    <string name="nav_open">打開</string>
    <string name="nav_close">關閉</string>
</resources>